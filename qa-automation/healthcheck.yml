---
# HEALTHCHECK — Quick environment readiness check (Satellite Lightspeed lab)
# Verifies all hosts are reachable and Satellite services are operational.
# Non-destructive: does not modify any lab state.
# Should complete in under 60 seconds.
#
# Required env vars: BASTION_HOST, BASTION_PORT, BASTION_USER, BASTION_PASSWORD
#
# Exit 0 = healthy, non-zero = unhealthy.

- name: Build inventory
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Add bastion (satellite) host
      ansible.builtin.add_host:
        name: "{{ lookup('ansible.builtin.env', 'BASTION_HOST') }}"
        ansible_ssh_host: "{{ lookup('ansible.builtin.env', 'BASTION_HOST') }}"
        ansible_ssh_port: "{{ lookup('ansible.builtin.env', 'BASTION_PORT') }}"
        ansible_ssh_user: "{{ lookup('ansible.builtin.env', 'BASTION_USER') }}"
        ansible_ssh_pass: "{{ lookup('ansible.builtin.env', 'BASTION_PASSWORD') }}"
        ansible_ssh_common_args: "-o StrictHostKeyChecking=no"
        groups: satellite

    - name: Add RHEL target hosts
      ansible.builtin.add_host:
        name: "{{ item }}"
        ansible_ssh_host: "{{ item }}"
        ansible_ssh_port: "{{ lookup('ansible.builtin.env', 'BASTION_PORT') }}"
        ansible_ssh_user: "{{ lookup('ansible.builtin.env', 'BASTION_USER') }}"
        ansible_ssh_pass: "{{ lookup('ansible.builtin.env', 'BASTION_PASSWORD') }}"
        ansible_ssh_common_args: "-o StrictHostKeyChecking=no"
        ansible_python_interpreter: /usr/libexec/platform-python
        groups: rhel_targets
      loop:
        - rhel1
        - rhel2

- name: Healthcheck - Satellite host
  hosts: satellite
  gather_facts: false
  become: true
  tasks:
    - name: Verify SSH connectivity
      ansible.builtin.ping:

    - name: Verify Satellite services are running
      ansible.builtin.command:
        cmd: hammer ping
      changed_when: false

    - name: Verify Satellite Web UI is responding
      ansible.builtin.uri:
        url: https://localhost/users/login
        validate_certs: false
        status_code: [200, 302]
      changed_when: false

    - name: Check iop containers are absent (Lightspeed not pre-installed)
      ansible.builtin.shell:
        cmd: podman ps --format '{{ '{{' }}.Names{{ '}}' }}' | grep -qi iop
      register: _iop_check
      changed_when: false
      failed_when: _iop_check.rc == 0

    - name: Verify egress to security.access.redhat.com
      ansible.builtin.uri:
        url: https://security.access.redhat.com
        method: HEAD
        status_code: [200, 301, 302, 403]
        timeout: 15

- name: Healthcheck - RHEL target hosts
  hosts: rhel_targets
  gather_facts: false
  become: false
  tasks:
    - name: Verify SSH connectivity
      ansible.builtin.ping:
